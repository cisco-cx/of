# secrets.yaml
# Eventually this will be a Kubernetes secret

apic:
  cluster:
    name: lab-aci

  ## NOTE: The following are not yet implemented to read from this file.
  ## Until that is done, just use env vars: APIC_HOSTS, APIC_USER, APIC_PASS.
  # host: x.x.x.x  # IPV4, IPV6, or DNS Name
  # credentials:
  #   username: <username>
  #   password: <password>
